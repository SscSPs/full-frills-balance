---
description: this is a workflow to create commit message for staged changes
---

go over the git diff --cached to see whats changed and do a code review and if all looks good, give me a commit message, otherwise give me what should be improved
